# FFXIV Servertime Discord bot
A discord bot that shows the servertime from ffxiv as the name of the bot.
So you know what ServerTime it is when scheduling events and such without needing to open the game.

# How to run
Create a file Token.env
and inside add your token like this DISCORD_TOKEN={YOUR TOKEN}
then run it by writing docker-compose up -d in terminal
